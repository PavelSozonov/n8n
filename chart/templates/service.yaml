apiVersion: v1
kind: Service
metadata:
  name: {{ include "n8n.fullname" . }}
  labels:
    app: {{ include "n8n.name" . }}
spec:
  type: ClusterIP
  selector:
    app: {{ include "n8n.name" . }}
  ports:
    - port: 80
      targetPort: 5678
